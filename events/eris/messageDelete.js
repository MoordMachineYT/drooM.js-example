const drooM = require("droom.js");

function messageDelete(droom, param1, param2, param3) { // Feel free to modify the parameter names and the variable names
  const client = droom._client;
}

module.exports = messageDelete;
